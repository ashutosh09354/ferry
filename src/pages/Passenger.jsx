import React from 'react';
import {useNavigate} from 'react-router-dom';
import {ArrowRight} from 'lucide-react';
import {PageShell,Summary} from '../components/Common';
export default function Passenger(){const nav=useNavigate();return <PageShell kicker="Step 1 of 2" title="Passenger information"><div className="mt-8 grid gap-7 lg:grid-cols-[1.5fr_.8fr]"><form onSubmit={event=>{event.preventDefault();const formData=new FormData(event.currentTarget);localStorage.setItem('ferryPassengerName',String(formData.get('name')).trim());nav('/payment')}} className="rounded-2xl border bg-white p-6"><div className="grid gap-4 sm:grid-cols-2"><input className="field" name="name" placeholder="Full name" aria-label="Full name" required/><input className="field" name="mobile" type="tel" inputMode="numeric" pattern="[0-9+() -]{7,}" placeholder="Mobile number" aria-label="Mobile number" required/><input className="field" name="email" type="email" placeholder="Email address" aria-label="Email address" required/><select className="field" name="idType" defaultValue="" aria-label="ID type" required><option value="" disabled>ID type</option><option>Aadhaar</option><option>Passport</option><option>Driving licence</option></select><input className="field sm:col-span-2" name="idNumber" placeholder="ID number" aria-label="ID number" required/></div><label className="mt-5 flex gap-2 text-sm text-slate-600"><input type="checkbox" defaultChecked/>Save details for future bookings</label><button type="submit" className="btn-primary mt-7">Continue to payment <ArrowRight size={17}/></button></form><Summary/></div></PageShell>}
